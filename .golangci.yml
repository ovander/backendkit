run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - misspell
    - goimports
    - revive
  disable:
    - depguard

linters-settings:
  revive:
    rules:
      - name: exported
        disabled: false
