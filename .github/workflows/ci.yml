name: CI

on:
  push:
    branches: [main, "feat/**"]
  pull_request:

jobs:
  test:
    name: Build & Test
    runs-on: ubuntu-latest
    strategy:
      matrix:
        go-version: ["1.25"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Go ${{ matrix.go-version }}
        uses: actions/setup-go@v5
        with:
          go-version: ${{ matrix.go-version }}
          cache: true

      - name: Verify go.sum is up to date
        run: |
          go mod tidy
          git diff --exit-code go.sum

      - name: Build (all packages)
        run: go build ./...

      - name: Run tests
        run: go test -race -count=1 -timeout=120s ./...

      - name: Vet
        run: go vet ./...

  lint:
    name: golangci-lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.25"
          cache: true
      - uses: golangci/golangci-lint-action@v7
        with:
          version: v2.5.0
          # Skip the action's `config verify` step: it fetches a JSON schema from
          # golangci-lint.run at runtime, which makes CI fail on transient network
          # timeouts unrelated to the code. Local `golangci-lint run` still
          # validates the config.
          verify: false
